
This example demonstrates a camel route that makes use of web services and CXF's Transformation Feature to change the outgoing namespace.

Once compiled and deployed, the sample web service can be invoked at http://localhost:8080/EndpointService/EndpointPort
