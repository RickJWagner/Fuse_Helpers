
This example shows how to use a RoutePolicy to determine when a primary Exchange is completed (if the primary uses multicast, and thus spawns child Exchanges).

It also shows that EventNotifiers do not work as we would like (using Correlation Id).


